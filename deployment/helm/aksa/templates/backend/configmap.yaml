apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-backend-configmap
  namespace: {{ .Release.Namespace }}
data:
  AKSA_SERVER_PORT: {{ .Values.backend.config.serverPort | quote }}
  AKSA_BASE_PATH: {{ .Values.backend.config.basePath | quote }}
  AKSA_SERVER_NATS_URL: {{ .Values.backend.config.natsUrl | quote }}
  {{- range $key, $val := .Values.backend.extraConfig }}
  {{ $key | upper }}: {{ $val | quote }}
  {{- end }}

